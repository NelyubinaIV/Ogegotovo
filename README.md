# 🎓 Портал подготовки к ОГЭ по русскому языку

Современный адаптивный сайт-портал для подготовки к ОГЭ с тёмной темой, системой прогресса, конфетками и аналитикой ошибок.

## ✨ Основные возможности

### Для учеников
- 🔑 **Вход без регистрации** по персональной ссылке-токену
- 📚 **10 уроков** с видео, материалами и заданиями
- 🍬 **Система наград** — конфетки за выполнение заданий
- 📊 **Личный прогресс** — отслеживание успехов
- 🎯 **Отслеживание ошибок** — чек-лист по категориям
- 🏆 **Достижения** и статистика

### Для учителей
- 👥 **Список учеников** с прогрессом и активностью
- 📈 **Аналитика класса** — топ ошибок, динамика
- 🔍 **Детальная карточка ученика** с историей результатов
- 📥 **Экспорт данных** в CSV и JSON
- 🎯 **Анализ ошибок** по темам и категориям

## 🚀 Запуск локально

### Требования
- Node.js 16+ и npm/pnpm

### Установка и запуск
```bash
# Установка зависимостей
npm install
# или
pnpm install

# Запуск в режиме разработки
npm run dev
```

Откройте [http://localhost:5173](http://localhost:5173) в браузере.

## 📦 Сборка для продакшена

```bash
npm run build
```

Собранные файлы будут в папке `dist/`.

## 🌐 Развёртывание на GitHub Pages

1. Создайте репозиторий на GitHub
2. Загрузите код:
```bash
git init
git add .
git commit -m "Initial commit"
git branch -M main
git remote add origin https://github.com/ваш-username/ваш-repo.git
git push -u origin main
```

3. Настройте GitHub Pages:
   - Зайдите в Settings → Pages
   - Source: выберите "GitHub Actions"
   
4. Создайте файл `.github/workflows/deploy.yml`:
```yaml
name: Deploy to GitHub Pages

on:
  push:
    branches: [ main ]

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      
      - name: Setup Node
        uses: actions/setup-node@v3
        with:
          node-version: 18
          
      - name: Install dependencies
        run: npm install
        
      - name: Build
        run: npm run build
        
      - name: Deploy
        uses: peaceiris/actions-gh-pages@v3
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./dist
```

5. Сайт будет доступен по адресу: `https://ваш-username.github.io/ваш-repo/`

## ⚙️ Настройка и редактирование

### Изменение уроков

Откройте файл `/src/config/lessons.ts`:

```typescript
export const LESSONS: Lesson[] = [
  {
    id: 1,
    title: 'Название урока',
    description: 'Описание урока',
    videoUrl: 'https://youtube.com/...', // Ссылка на видео
    materialUrl: 'https://docs.google.com/...', // Ссылка на материалы
    tasks: [
      {
        id: 'l1-t1',
        name: 'Название задания',
        type: 'external',
        url: 'https://forms.google.com/...', // Ссылка на задание
        reward: 2, // Количество конфет
        maxScore: 10, // Максимальный балл
      },
    ],
    totalReward: 2, // Общее количество конфет за урок
    unlockType: 'date', // 'date' | 'progress' | 'both'
    unlockDate: '2024-01-01', // Дата открытия (опционально)
    requiredLessons: [], // Требуемые уроки (опционально)
  },
  // ... остальные уроки
];
```

### Изменение словаря ошибок

Откройте файл `/src/config/mistakes.ts` и добавьте/измените ошибки:

```typescript
export const MISTAKES: Mistake[] = [
  {
    id: 'ORTH_PRE_PRI',
    category: 'ORTH', // ORTH | PUNCT | GRAM | SPEECH | TEXT
    name: 'ПРЕ-/ПРИ-',
    description: 'Правописание приставок ПРЕ- и ПРИ-',
    tags: ['приставки', 'орфография'],
  },
  // ... остальные ошибки
];
```

### Изменение ключа учителя

Откройте файл `/src/app/App.tsx` и измените константу:

```typescript
const TEACHER_SECRET_KEY = 'ВАШ-СЕКРЕТНЫЙ-КЛЮЧ';
```

## 👨‍🎓 Как работать с учениками

### Выдача токенов ученикам

**Вариант 1: Генерация токена вручную**

Используйте формат: `XXXX-YYYY` (например, `ABCD-1234`).

Отправьте ученику персональную ссылку:
```
https://ваш-сайт.com/?token=ABCD-1234
```

**Вариант 2: Генерация прямой ссылки**

Используйте hash-формат:
```
https://ваш-сайт.com/#/u/ABCD-1234
```

### Вход учителя

**Через URL с ключом:**
```
https://ваш-сайт.com/teacher?key=TEACHER-OGE-2025
```

**Через форму входа:**
1. Откройте главную страницу
2. Нажмите "Войти как учитель"
3. Введите ключ: `TEACHER-OGE-2025`

## 📝 Как работает фиксация результатов

### Режим 1: Автоматический (будущая версия с Supabase)

В будущем можно будет принимать результаты через API:

```
POST /result
{
  "token": "ABCD-1234",
  "lessonId": "1",
  "taskId": "l1-t1",
  "score": 8,
  "max": 10,
  "passed": true,
  "attempt": 1,
  "mistakes": ["ORTH_PRE_PRI", "PUNCT_SSP"]
}
```

Или через `postMessage` для встраивания:

```javascript
window.parent.postMessage({
  type: 'oge_result',
  data: { /* результат */ }
}, '*');
```

### Режим 2: Ручной (работает сейчас)

Ученик нажимает кнопку **"🍬 Забрать конфетку"** → заполняет форму:

1. **Баллы** (0-максимум)
2. **Статус** (Пройдено/Не сдано)
3. **Чек-лист ошибок** (по категориям)
4. **Заметки** (опционально)
5. **Скриншот** (ссылка, опционально)

После отправки:
- Результат сохраняется
- Начисляются конфетки (если пройдено впервые)
- Обновляется прогресс урока

## 🗄️ Хранение данных

### Текущая версия: localStorage

Все данные хранятся локально в браузере:
- `oge_student_token` — токен ученика
- `oge_student_data` — прогресс ученика
- `oge_all_students` — список всех учеников
- `oge_teacher_key` — ключ учителя

**⚠️ Ограничения:**
- Данные привязаны к браузеру
- При очистке браузера данные теряются
- Учитель видит только учеников с этого устройства

### Рекомендуемая версия: с Supabase (опцио��ально)

Для полноценной работы рекомендуется подключить Supabase:

1. Создайте проект на [supabase.com](https://supabase.com)
2. Создайте таблицы:
   - `students` — данные учеников
   - `results` — результаты заданий
3. Настройте RLS (Row Level Security)
4. Добавьте переменные окружения в проект

**Преимущества:**
- Данные сохраняются в облаке
- Доступ с любого устройства
- Учитель видит всех учеников
- Резервное копирование

## 🎨 Дизайн и тема

Проект использует тёмную тему с акцентными цветами:

- **Фон:** Графит (#0f1419)
- **Карточки:** Ночной синий (#1a1f2e)
- **Акценты:**
  - Бирюзовый (#14b8a6) — основной
  - Золото (#fbbf24) — конфетки, награды
  - Оранжевый (#fb923c) — прогресс

Для изменения цветов отредактируйте `/src/styles/theme.css`.

## 📱 Адаптивность

Сайт полностью адаптивен и отлично работает на:
- 📱 Мобильных телефонах (320px+)
- 📱 Планшетах (768px+)
- 💻 Десктопах (1024px+)

## 🔧 Технологии

- **React 18** — фреймворк
- **TypeScript** — типизация
- **Tailwind CSS 4** — стилизация
- **Radix UI** — компоненты UI
- **Recharts** — графики и диаграммы
- **Lucide React** — иконки
- **Sonner** — уведомления
- **Vite** — сборщик

## 📄 Лицензия

MIT License — свободное использование.

## 🤝 Поддержка

Если нужна помощь:
1. Проверьте консоль браузера (F12)
2. Убедитесь, что используете современный браузер
3. Проверьте, что localStorage включён

---

**Успехов в подготовке к ОГЭ! 🎓📚**
